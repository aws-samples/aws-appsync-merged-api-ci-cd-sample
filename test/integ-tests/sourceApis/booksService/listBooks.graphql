query listBooks {
    listBooks(limit: 100) {
        items {
            id
            title
            id
            authorId
            genre
            publicationYear
            publisherId
        }
    }
}